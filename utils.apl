:NameSpace Utils

Ⓤ ← {⍺←⊢ ⋄ (⍵⍵⍣¯1)⍺(⍺⍺⍥⍵⍵)⍵}
Ⓞ ← {⍺←⊢ ⋄ ((⍺⍺ ⍺)⍵⍵⊢)⍵}

Format ← {
    m←('`'∘=)⍵
    c←1⌈+/∧\m
    w←0,(c⍴1),0
    p←((w≡⊢)⌺(≢w))m
    b←(∧\Ⓤ⌽≠\)Ⓞ<p
    ⊃⍵⊆⍨~∨⌿(c↑0 1 ¯1) (⌽⍤0 1) b
}

:EndNameSpace